# ─────────────────────────────────────────────
# MarkMaster — Requirements
# ─────────────────────────────────────────────

# UI
streamlit>=1.35.0

# LLM API
groq>=0.9.0

# Sentence Embeddings (used in matcher.py + vector_store.py)
sentence-transformers>=2.7.0

# BERTScore (used in matcher.py)
bert-score>=0.3.13

# Environment variables
python-dotenv>=1.0.0

# ─────────────────────────────────────────────
# RAG dependencies
# ─────────────────────────────────────────────

# Vector database — pinned to 0.4.24 to avoid opentelemetry gRPC crash
# on Streamlit Cloud (chromadb>=0.5 pulls broken opentelemetry-exporter-otlp-proto-grpc)
chromadb==0.4.24

# Pin opentelemetry stack that chromadb 0.4.24 depends on — prevents TypeError on import
opentelemetry-api==1.23.0
opentelemetry-sdk==1.23.0
opentelemetry-exporter-otlp-proto-grpc==1.23.0
opentelemetry-instrumentation-fastapi==0.44b0

# PDF loading (loader.py)
pymupdf>=1.24.0

# Word document loading (loader.py)
python-docx>=1.1.0

# PowerPoint loading (loader.py)
python-pptx>=0.6.23

numpy<2.0